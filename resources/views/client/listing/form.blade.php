<div class="col-md-6 form-elem">
    <div class="default-inp form-elem">
        <label for="loading">{{trans('listing.loading')}}</label>
        <input type="text" name="loading" id="loading"
               placeholder="{{trans('listing.loading')}}">
    </div>
    <div class="default-inp form-elem">
        <label for="unloading">{{trans('listing.unloading')}}</label>
        <input type="text" name="unloading" id="unloading"
               placeholder="{{trans('listing.unloading')}}">
    </div>
    <div class="default-inp form-elem">
        <label for="length">{{trans('listing.length')}}</label>
        <input type="text" name="length" id="length"
               placeholder="{{trans('listing.length')}}">
    </div>
    <div class="default-inp form-elem">
        <label for="weight">{{trans('listing.weight')}}</label>
        <input type="text" name="weight" id="weight"
               placeholder="{{trans('listing.weight')}}">
    </div>
    <div class="default-inp form-elem">
        <label for="volume">{{trans('listing.volume')}}</label>
        <input type="password" name="volume" id="volume"
               placeholder="{{trans('listing.volume')}}">
    </div>
    <div class="default-inp form-elem">
        <label for="lorry_type">{{trans('listing.lorry_type')}}</label>
        <input type="text" name="lorry_type" id="lorry_type"
               placeholder="{{trans('listing.lorry_type')}}">
    </div>
</div>